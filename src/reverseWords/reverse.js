class Word {
    /**
     * 
     * @param {String} text Stdin that is a string containg a sentence or word
     * @returns The input but reversed
     */
    reverseWord(text){
        return text;
    }
}

module.exports = Word;